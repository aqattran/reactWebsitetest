import React from 'react'
const headerStyle = {
  backgroundColor:'lightblue',
  color: '#fff',
};

const header = () => {
  return (
    <header style ={headerStyle}>
        <h1>
            Groceries List
        </h1>
    </header>
  )
}

export default header